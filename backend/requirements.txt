# Atlas backend — Phase 1 runtime + test dependencies.
# ML/forecasting libs (scikit-learn, xgboost, lightgbm, prophet) are intentionally NOT here yet;
# they arrive with the Phase 4 learning layer to keep the base install fast.
fastapi~=0.115
uvicorn[standard]~=0.30
SQLAlchemy~=2.0
pydantic~=2.7
pydantic-settings~=2.3
python-dateutil~=2.9
alembic~=1.13

# --- tests ---
httpx~=0.27
pytest~=8.2
